import { StyleSheet, Text, View } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    marginBottom: 16,
  },
  label: {
    fontSize: 15,
    color: '#C3C3C3',
    marginBottom: 6,
  },
  input: {
    height: 56,
    borderColor: '#C16897',
    borderWidth: 1,
    borderRadius: 50,
    paddingLeft: 22,
  }
});
